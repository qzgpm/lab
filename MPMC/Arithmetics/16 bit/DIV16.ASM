ASSUME CS:CODE, DS:DATA

DATA SEGMENT
    N1 DW 0032H
    N2 DW 0008H
    QUO DW ?
    REM DW ?
DATA ENDS

CODE SEGMENT
START:
    MOV AX, DATA
    MOV DS, AX

    MOV AX, N1
    XOR DX, DX
    DIV N2

    MOV QUO, AX
    MOV REM, DX

    MOV AH, 4Ch
    INT 21h
CODE ENDS
END START
